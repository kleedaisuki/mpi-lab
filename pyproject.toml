[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mpi-svd-lab"
version = "0.1.0"
description = "MPI SVD lab project scaffold."
readme = "README.md"
requires-python = ">=3.10"
